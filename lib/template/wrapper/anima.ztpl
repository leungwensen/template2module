function (data, helper) {
    data = data || {};
    helper = helper || {};

    return (function ({%= data.formalArguments %}) {
        {%= data.functionBody %}
    })({%= data.realArguments %});
}

